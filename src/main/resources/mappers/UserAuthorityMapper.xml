<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.khBootAdmin.mapper.UserAuthorityMapper">

    <insert id="save" parameterType="UserAuthorityVO">
        INSERT INTO USER_AUTHORITY (USER_ID, ROLE_ID)
        VALUES (#{userId}, #{roleId})
    </insert>

    <select id="findRolesByUserId" parameterType="long" resultType="kr.co.khBootAdmin.model.Role">
        SELECT r.ROLE_ID as id, r.ROLE_NAME as role
        FROM USER_AUTHORITY ua
        JOIN ROLE r ON ua.ROLE_ID = r.ROLE_ID
        WHERE ua.USER_ID = #{userId}
    </select>

    <delete id="deleteUserAuthorityByUserId" parameterType="Long">
        DELETE FROM USER_AUTHORITY WHERE USER_Id = #{userId}
    </delete>


</mapper>